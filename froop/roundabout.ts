import {roundaboutOptions} from './types';

export async function roundabout<TProps = any, TActions = TProps>(
    vm: TProps & TActions, options: roundaboutOptions<TProps, TActions>){
    
}

export class RoundAbout{
    
}